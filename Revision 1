#include<stdio.h>
struct Process_Details
{
    int allocated,max_req;
};
int main(void)
{
    int i,j,x;
    int total_resource[3];
    int completed[5][3];
    int remaining_resource[3];
    struct Process_Details P[5][3];

    for(i=0;i<3;i++)
    {
        printf("Enter the Total resources for Instance %c\n",(char)(i+65));
        scanf("%d",&total_resource[i]);
    }
}
